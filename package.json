{
  "name": "kidrove-workshop-root",
  "version": "1.0.0",
  "description": "Root configuration for Kidrove Workshop Landing Page project",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run dev --prefix backend",
    "start": "npm start --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
